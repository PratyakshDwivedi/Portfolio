# ─────────────────────────────────────────────────────────────
# React Bits Pro registry license key.
# Get yours at https://pro.reactbits.dev — copy this file to `.env`
# and paste your key below. `.env` is gitignored; never commit it.
#
# This key authorizes `npx shadcn@latest add @reactbits-starter/...`
# to pull Pro components (Particle Text, Liquid ASCII, Circle/Infinite/
# Reel galleries). See components.json for the registry wiring.
# ─────────────────────────────────────────────────────────────
REACTBITS_LICENSE_KEY=
