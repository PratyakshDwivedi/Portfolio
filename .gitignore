# dependencies
node_modules
.pnp
.pnp.js

# build
dist
dist-ssr
*.local
*.tsbuildinfo

# env — NEVER commit real secrets (React Bits Pro license key, etc.)
.env
.env.local
.env.*.local

# editor / os
.DS_Store
.vscode/*
!.vscode/extensions.json
Thumbs.db

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local Claude Code settings (machine-specific permissions, not shared config)
.claude/settings.local.json
